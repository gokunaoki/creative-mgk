import React from "react";
import styled from "styled-components";

const Text = styled.h2`
  color: white;
  font-size: 1.6rem;
  font-weight: bold;
  display: inline;
`;
const Logo = () => {
  return <Text>MGK OFFICIAL</Text>;
};

export default Logo;
